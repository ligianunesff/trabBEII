const app = require('../app'); // Importa o aplicativo Express
const http = require('http');

const port = process.env.PORT || 3000; // Obtém a porta do ambiente ou usa a porta 3000 como padrão
app.set('port', port);

const server = http.createServer(app);

server.listen(port); // Inicia o servidor na porta especificada
